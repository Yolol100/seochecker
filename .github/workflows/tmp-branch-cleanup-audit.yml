name: Temporary branch cleanup audit

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This temporary workflow is not part of the product change and will be removed before PR."
